# Server Configuration Files

Place your Firebase Service Account private key JSON file in this directory with the filename:
`firebase-service-account.json`

Alternatively, you can specify a custom path in your `.env` file:
```env
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/your/firebase-service-account.json
```
